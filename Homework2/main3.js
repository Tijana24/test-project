//selection thing that we are going to work with
const textCounter= document.getElementById('textField');
const buttonElem= document.getElementById('btn');
buttonElem.addEventListener('click', handlerClikButton);
console.log(buttonElem);

//event handlers
function handlerClikButton(){
    
}
